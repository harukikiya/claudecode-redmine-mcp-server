# Redmine MCP Server — 環境変数設定例
# このファイルをコピーして .env を作成し、実際の値を設定する
# .env は .gitignore に追加済み（コミットしないこと）

# Redmine サーバーの URL（末尾スラッシュなし）
REDMINE_URL=http://localhost:3000

# Redmine API キー（マイアカウント → APIアクセスキーで確認）
REDMINE_API_KEY=your_api_key_here

# HTTP タイムアウト秒数（省略時: 30.0）
# REDMINE_TIMEOUT=30.0
